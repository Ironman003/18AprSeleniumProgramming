package exploreWebdriver;

public class lenghtyWork {
	private static String storedString = "Here window handle is a unique identifier that stores the values of \n"
			+ "windows opened on a webpage and helps in window handling in Selenium.\n"
			+ "getWindowHandles( ) and getWindowHandles( ) handle windows in Selenium.\n"
			+ "The user has to switch from the parent window to the child window to work\n"
			+ "on them using switchTo( ) method.";

	public static void storedString(String googleId) {
		System.out.println(storedString+"\n Unique ID is = "+googleId);
	}

}
